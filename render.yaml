services:
  - type: web
    name: casino-promo-site
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_PATH
        value: /usr/local/bin/python